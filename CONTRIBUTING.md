# Contributing to SonicMart

First off, thanks for taking the time to contribute to SonicMart! 🎉  
We welcome contributions from the audio community, whether it's fixing bugs, adding new component specs, or improving the documentation.

## 🛠️ How to Contribute

### 1. Reporting Bugs
- Go to the **Issues** tab.
- Click **New Issue**.
- Provide a clear title and description.
- Include screenshots or error logs if possible.

### 2. Suggesting Enhancements
- Open a **New Issue** with the label `enhancement`.
- Describe the feature you'd like to see (e.g., "Add filter for Capacitor Voltage").

### 3. Pull Requests (PRs)
1.  **Fork** the repository to your own GitHub account.
2.  **Clone** your fork locally:
    ```bash
    git clone https://github.com/your-username/SonicMart.git
    ```
3.  **Create a new branch** for your feature:
    ```bash
    git checkout -b feature/AmazingFeature
    ```
4.  **Make your changes** and commit them:
    ```bash
    git commit -m "feat: Add new amplifier module specs"
    ```
5.  **Push** to your fork:
    ```bash
    git push origin feature/AmazingFeature
    ```
6.  Open a **Pull Request** on the main SonicMart repository.

## 💻 Development Setup

Please refer to the [README.md](./README.md#getting-started) for instructions on how to set up the project locally.

## 📜 Code Style
- Use **ES6+** syntax.
- Ensure your code is formatted (Prettier is recommended).
- Add comments for complex logic, especially in the `backend/controllers`.

---
**Happy Coding!** 🎧
